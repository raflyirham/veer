import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Veer | Sign Up",
  description: "Sign up for a Veer account",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
